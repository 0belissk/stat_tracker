#!/bin/sh
set -e

if ! command -v mvn >/dev/null 2>&1; then
  echo "System Maven (mvn) is required but not installed" >&2
  exit 1
fi

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec mvn -f "$SCRIPT_DIR/pom.xml" -Dmaven.multiModuleProjectDirectory="$SCRIPT_DIR" "$@"
